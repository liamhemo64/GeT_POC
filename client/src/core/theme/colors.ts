/**
 * Flat color constants — re-exported from tokens.ts.
 * Use these when you need a raw hex string (e.g. StatusBar, LinearGradient).
 * For Tamagui components, prefer the `$color.*` tokens instead.
 */
export { colors } from './tokens'
